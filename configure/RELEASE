#RELEASE Location of external products
# Run "gnumake clean uninstall install" in the application
# top directory each time this file is changed.

EPICS_EXTENSIONS_RELEASE=/afs/slac/g/lcls/build/epics/extensions/extensions-R3-14-8-2
EPICS_MODULES=/afs/slac/g/lcls/build/epics/modules

#If using macro substitutions, point MSI at the binary:
MSI=$(EPICS_EXTENSIONS_RELEASE)/bin/$(EPICS_HOST_ARCH)/msi

#  IPAC release V2-7 or later is required.
IPAC=$(EPICS_MODULES)/ipac/ipac-R1-0-2

# EPICS_BASE usually appears last so other apps can override stuff:
EPICS_BASE_RELEASE=/afs/slac/g/lcls/build/epics/base/base-R1-0-0
EPICS_BASE=$(EPICS_BASE_RELEASE)
